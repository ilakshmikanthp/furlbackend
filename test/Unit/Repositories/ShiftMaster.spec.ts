import { ShiftMasterRepository } from '../../../src/Repositories/ShiftMaster';

describe('Test ShiftMasterRepository constructor', () => {
	// it('should create an empty options', () => {
	// 	expect(ShiftMasterRepository).toBeDefined();
	// });

	// it('should work (method name)', async () => {
	// 	const result = await ShiftMasterRepository.get(1);
	// 	expect(result).toBeDefined();
	// });
});
